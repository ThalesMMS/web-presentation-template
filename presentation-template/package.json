{
  "name": "presentation-template",
  "version": "1.0.0",
  "private": true,
  "description": "Esqueleto de apresentação HTML com votação ao vivo.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
