{
  "name": "presentation-template",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive web presentation template with live polls, messages, audience exams, and a control room.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
